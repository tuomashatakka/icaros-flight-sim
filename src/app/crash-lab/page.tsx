import { CrashLab } from '@/components/crash-lab'


export const metadata = {
  title:       'Crash Lab',
  description: 'Deterministic physics crash-dummy cases, played back frame by frame.',
}

export default function CrashLabPage () {
  return <CrashLab />
}
